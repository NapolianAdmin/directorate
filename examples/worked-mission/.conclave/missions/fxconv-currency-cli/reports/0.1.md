POST: Architect
WAVE: 0  ORDER: 0.1

STATUS: DONE
ORDER: 0.1

WHAT I CHANGED
- fxconv/__init__.py: package docstring + `__version__`, no logic.
- fxconv/__main__.py: `python -m fxconv` wiring that calls `fxconv.cli.main()` (trivial glue, not a stub — nothing to make `NotImplementedError` for).
- fxconv/rates.py: `fetch_rate(from_currency, to_currency, timeout=10.0) -> RateResult` stub (`raise NotImplementedError`), plus `RatesError` base, `InvalidCurrencyCode`, `RateFetchError` exception classes and a `RateResult` NamedTuple, all fully documented with the urllib.error → exception-type translation implementers must perform.
- fxconv/cache.py: `CacheEntry` frozen dataclass, `CURRENCY_CODE_RE = re.compile(r"^[A-Z]{3}$")`, `_cache_path()`, `get()`, `set()`, `is_fresh()` — all stubs raise `NotImplementedError`. Module and `_cache_path()` docstrings spell out the mandatory allowlist-then-path strategy (validate against `CURRENCY_CODE_RE` and raise `ValueError` as the first statement, before any path is built) so a code like `"../../etc/passwd"` can never reach a path-join call.
- fxconv/cli.py: `build_parser()`, `convert(from_currency, to_currency, amount, ttl_seconds=...)`, `main(argv=None) -> int` stubs. `convert()`'s docstring specifies the exact cache-then-fetch wiring (including that the fresh-cache-hit path must not call `rates.fetch_rate()`, since the test suite will assert zero extra calls). `main()`'s docstring requires catching `rates.RatesError` and exiting non-zero with a clean stderr message, no raw traceback.

VERIFICATION OUTPUT
```
$ python -c "import fxconv.rates, fxconv.cache, fxconv.cli"
IMPORT_OK

$ python -m py_compile fxconv/__init__.py fxconv/__main__.py fxconv/rates.py fxconv/cache.py fxconv/cli.py
COMPILE_OK

$ wc -l fxconv/*.py
   16 fxconv/__init__.py
   12 fxconv/__main__.py
  121 fxconv/cache.py
   74 fxconv/cli.py
   81 fxconv/rates.py
  304 total
```
AST check confirmed every real function stub (`fetch_rate`, `_cache_path`, `get`, `set`, `cli.build_parser`, `cli.convert`, `cli.main`) has exactly one statement in its body: `raise NotImplementedError`. `is_fresh` similarly stubs. Exception classes and the `CacheEntry`/`RateResult` data types are declaration-only (no bodies to stub).

WHAT I DID NOT DO
- Did not touch tests/ or .conclave/.
- Did not implement any real logic — every function body is `raise NotImplementedError` as required.
- Did not run `pytest` (no tests exist yet at this wave; that's wave 1's job).

RISKS AND UNKNOWNS
- Total size is 304 lines, roughly 2x the "well under 150" budget note. The overage is concentrated in cache.py's PATH SAFETY documentation and the exception-type contracts in rates.py — both are the mission's explicit "real failure surface" (path traversal, no-network-on-cache-hit), so I prioritized unambiguous instructions for three parallel implementers over hitting the line count. Flagging this rather than silently overshooting.
- `_cache_path` naming the file `f"{from_currency}_{to_currency}.json"` assumes 1.2's implementer follows this exactly; since the file name format itself isn't security-relevant (only the validate-before-join ordering is), I left it as a suggestion implementers can keep or adjust without breaking the safety contract, but 1.1/1.3 callers don't touch this format directly so no cross-file coupling risk.
- `cache.DEFAULT_CACHE_DIR = Path.home() / ".cache" / "fxconv"` is a Windows-unfriendly-looking default (no `%LOCALAPPDATA%` handling) but this repo's test suite will almost certainly override `cache_dir` explicitly per test (e.g. via `tmp_path`), so I did not over-engineer a cross-platform default at stub time — worth 1.2's implementer double-checking.
- `rates.fetch_rate`'s signature takes `timeout` as a keyword with a default; if 1.3's `cli.convert` needs to pass a custom timeout end-to-end it isn't currently plumbed through `convert()` — out of scope for stubs, easy to add in wave 1 if needed.

BLOCKERS
None.

LESSON CANDIDATE
When a work order's budget guidance ("well under 150 lines") collides with a requirement that demands the docstring spell out a specific security mechanism in detail ("state exactly how... e.g. validate against an allowlist pattern"), the security-documentation requirement should win and the deviation should be reported explicitly rather than trimmed to fit — future work orders combining a tight line budget with a security-sensitive interface-stub ask could soften the budget number when both instructions are given, so implementers don't have to choose which one to violate.

--- Chief verification (independent) ---
Re-ran `python -c "import fxconv.rates, fxconv.cache, fxconv.cli"` and `wc -l` myself;
matched exactly. Read fxconv/cache.py in full; the path-safety docstring matches what the
report claims. Checkpointed as bf20206.
